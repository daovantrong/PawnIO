#pragma once

// AMX VM architecture abstraction wrapper
// Provides a unified interface for different architectures

#include "arch_types.h"

// Note: amx_wrapper.h is included but not actually used
// The actual amx type is defined in vm.cpp with proper memory manager
